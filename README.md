## BB Dacha 2024 | Schedule bot

Simple bot for everyday schedule notifications.

## Installation

Assuming `uv` is your package manager:

```bash
uv pip install -r pyproject.toml
```

## Usage

```bash
python -m app.main
```

This will start the bot and log dispatcher updates to stdout.
